{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The launcher window: its own commands, events, and file pickers",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
